# Crystal Lane — Web build

Godot 4.3 HTML5 export (single-thread). Open `index.html` through HTTP (not `file://`).

Rebuild with `./export-web.sh` from the repo root. The committed tree may omit large `.wasm` binaries; use the public zip for a ready-to-host pack.
