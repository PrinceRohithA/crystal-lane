# Crystal Lane — Web build

Godot 4.3 HTML5 export (single-thread). Open `index.html` through HTTP (not `file://`).

```bash
python3 ../../scripts/serve_web.py --port 43180
```

CC0 Kenney + MELLE art. Original geometric placeholders as fallback. Not affiliated with Nintendo, Game Freak, or The Pokémon Company.
